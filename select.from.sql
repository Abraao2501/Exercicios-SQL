/*SELECT => Retorna uma coluna de uma tabela especificada pelo FROM*/

SELECT *
FROM  person.Person;

SELECT Title, NameStyle
FROM person.person;

SELECT *
FROM person.EmailAddress

SELECT FirstName, LastName
FROM person.Person